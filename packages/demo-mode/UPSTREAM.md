# Upstream record

- Repository: https://github.com/osolmaz/pi-demo-mode
- Commit: `8f18a3802a786be797404eb0a4ee3cce8a522b75`
- License: MIT
- Local changes: none; `index.ts` only re-exports the pinned extension

The extension changes Pi's interactive presentation and sends demo prompts only when
`PI_DEMO_MODE=1` is set. The wrapper adds no state or runtime behavior.
