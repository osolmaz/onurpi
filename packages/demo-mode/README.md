# Pi Demo Mode

This private OnurPi package loads a pinned Pi Demo Mode commit. It starts the self-driving story
demo only when `PI_DEMO_MODE=1` is set.

OnurPi loads this wrapper from the local checkout. It is not published to npm.

Run `/reload` after changing the package or global settings.
