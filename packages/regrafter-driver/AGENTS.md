# @onurpi/regrafter-driver

- Keep Pi Regraft pinned to an exact reviewed release.
- Keep `/regraft` deterministic and keep Regrafter delegation explicit and approval-bound.
- Do not add Regraft model tools or weaken repository lease checks.
- Update `UPSTREAM.md` after reviewing a new pin or local extension adaptation.
- Run `npm run check`, `npm run slophammer`, and `git diff --check` before finishing.
