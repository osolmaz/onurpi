# @onurpi/workflows

- Keep Pi Workflows pinned to an exact reviewed release.
- Keep the wrapper private and thin; make workflow behavior changes upstream.
- Preserve upstream workflow run schemas and storage boundaries.
- Update `UPSTREAM.md` after reviewing a new pin.
- Run `npm run check`, `npm run slophammer`, and `git diff --check` before finishing.
