# Upstream record

- Repository: https://github.com/osolmaz/pi-workflows
- Package: `@osolmaz/pi-workflows@0.2.0`
- License: MIT
- Local changes: none; `index.ts` only re-exports the pinned extension

The extension registers workflow commands and a workflow control tool. Workflow runs are stored
under Pi's user data directory as documented upstream. The wrapper adds no state or runtime
behavior.
